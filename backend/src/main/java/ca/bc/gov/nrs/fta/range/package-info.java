/**
 * Range module — grazing and hay-cutting tenures, range usage, land base, and
 * range zones (legacy screens {@code FTA_6xx} / {@code FTA_100RANGE_TENURE}).
 *
 * <p>Range agreements are modelled on the same tenure spine as harvesting in the
 * legacy system, so this module depends on {@code ca.bc.gov.nrs.fta.shared} for
 * the forest-file/client core and must not reference {@code tenure} or
 * {@code mark} directly.
 */
package ca.bc.gov.nrs.fta.range;
